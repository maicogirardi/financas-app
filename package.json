{
  "name": "financas-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:firebase": "vite build --mode firebase",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "deploy:firebase": "npm run build:firebase && firebase-tools deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^12.11.0",
    "vue": "^3.5.30"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.5",
    "firebase-tools": "^15.13.0",
    "gh-pages": "^6.3.0",
    "vite": "^8.0.1"
  }
}
